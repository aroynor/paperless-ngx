# The UID and GID of the user used to run paperless in the container. Set this
# to your UID and GID on the host so that you have write access to the
# consumption directory.
USERMAP_UID=1000
USERMAP_GID=1000

# Additional languages to install for optical character recognition, separated by a
# whitespace. For all available languages, see
# https://packages.debian.org/search?keywords=tesseract-ocr-&searchon=names&suite=buster
# PAPERLESS_OCR_LANGUAGE=eng

# OCR settings
PAPERLESS_OCR_LANGUAGE=eng
PAPERLESS_OCR_LANGUAGES=eng nor

# Adjust this key if you plan to make paperless available publicly. It should
# be a very long sequence of random characters. You don't need to remember it.
PAPERLESS_SECRET_KEY=change-me-to-a-random-string

# Use this variable to set a timezone for the Paperless Docker containers. If not specified, defaults to UTC.
PAPERLESS_TIME_ZONE=Europe/Oslo

# The default language to use for OCR. Set this to the language most of your
# documents are written in.
# PAPERLESS_OCR_LANGUAGE=eng

# Set if accessing paperless via a domain subpath e.g. https://domain.com/paperless
# PAPERLESS_FORCE_SCRIPT_NAME=/paperless
# PAPERLESS_STATIC_URL=/paperless/static/

# Paperless URL (if behind reverse proxy)
# PAPERLESS_URL=https://paperless.yourdomain.com

# Admin user credentials - change these!
PAPERLESS_ADMIN_USER=admin
PAPERLESS_ADMIN_PASSWORD=admin
PAPERLESS_ADMIN_MAIL=admin@example.com

# Database settings (should match docker-compose.yml)
PAPERLESS_DBNAME=paperless
PAPERLESS_DBUSER=paperless
PAPERLESS_DBPASS=paperless

# Tika and Gotenberg for advanced document processing (optional)
# PAPERLESS_TIKA_ENABLED=1
# PAPERLESS_TIKA_ENDPOINT=http://tika:9998
# PAPERLESS_TIKA_GOTENBERG_ENDPOINT=http://gotenberg:3000

# Consumption settings
PAPERLESS_CONSUMER_POLLING=60
PAPERLESS_CONSUMER_DELETE_DUPLICATES=true
PAPERLESS_CONSUMER_RECURSIVE=true
PAPERLESS_CONSUMER_SUBDIRS_AS_TAGS=true

# Document handling
PAPERLESS_FILENAME_FORMAT={created_year}/{correspondent}/{title}
PAPERLESS_FILENAME_FORMAT_REMOVE_NONE=true

# Email settings (optional - configure if you want email notifications)
# PAPERLESS_EMAIL_HOST=smtp.gmail.com
# PAPERLESS_EMAIL_PORT=587
# PAPERLESS_EMAIL_HOST_USER=your-email@gmail.com
# PAPERLESS_EMAIL_HOST_PASSWORD=your-app-password
# PAPERLESS_EMAIL_USE_TLS=true
# PAPERLESS_EMAIL_FROM=paperless@yourdomain.com
